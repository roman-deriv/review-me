{
    "name": "submit_review",
    "description": "Provide a final evaluation for the GitHub pull request. This tool should be used after generating individual comments to create an overall summary and determine the final review action (approve, comment, or request changes).",
    "input_schema": {
        "type": "object",
        "properties": {
            "feedback": {
                "type": "string",
                "description": "Final thoughts and overall feedback on the Pull Request"
            },
            "event": {
                "type": "string",
                "enum": ["APPROVE", "COMMENT", "REQUEST_CHANGES"],
                "description": "The final review action to take"
            }
        },
        "required": ["feedback", "event"]
    }
}
